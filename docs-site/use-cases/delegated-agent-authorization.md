# Delegated Agent Authorization

Delegated authorization depends on DID documents that can publish verification methods, relationships, and services. This repository owns the method-level data model and update API.

Runtime orchestration for agents, resolver services, and local key custody belongs in the resolver/services repository or downstream applications.
