# DID Core 1.0 Conformance Matrix (0.6 Evidence)

**Standards baseline:** [W3C DID Core 1.0 Recommendation, 19 July 2022](https://www.w3.org/TR/2022/REC-did-core-20220719/). **Integration base:** `55dde88ce1e451587303b5f4fc388eef4bdb32f5`. **Implementation under test:** the exact clean `git HEAD` printed by `pnpm test:conformance`, not the integration base.

This matrix preserves requirement-level traceability. `PASS` means only that the cited test directly proves the stated behavior within its scope. Exact revision evidence comes from clean-tree command output recorded in PR or release evidence; this lane is not currently run or retained by CI. Immutable external/CI evidence remains tracked by [#446](https://github.com/midnightntwrk/midnight-did/issues/446). This document is neither W3C certification nor a claim of complete DID Core compatibility.

| ID | Requirement / W3C section | Midnight specification | Implementation location | Exact test | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| DID-001 | DID syntax is a DID URI (§3, §6). | §2 | `packages/domain/src/midnight.ts` (`MidnightDIDSchema`) | `packages/domain/src/test/midnight-did-syntax.conformance.test.ts` — `accepts ledger DID %s`; `accepts both offchain short and syntactically valid long forms` | PASS WITH RESTRICTION | Grammar is intentionally `did:midnight`-specific. |
| DID-002 | Method name and method-specific-id are defined (§6). | §2 ABNF | `packages/domain/src/midnight.ts` | `packages/domain/src/test/midnight-did-syntax.conformance.test.ts` — `accepts ledger DID %s`; `rejects $label` | PASS | Six ledger networks and `offchain` are explicit. |
| DID-003 | Malformed method identifiers are rejected (§6). | §2, §2.1 | `MidnightDIDSchema`; offchain state parser | `packages/domain/src/test/midnight-did-syntax.conformance.test.ts` — `rejects $label`; `rejects an offchain long form whose encoded state hash does not match` | PASS | Includes lengths, hex, network, and long-form hash checks. |
| DID-004 | Normalization and case behavior are defined (§6). | §2 | `packages/domain/src/midnight.ts`; `packages/did/src/midnight-did-resolver.ts` | `packages/domain/src/test/midnight-did-syntax.conformance.test.ts` — `canonicalizes ledger hexadecimal case to lowercase`; `packages/did/src/test/midnight-did-resolver.test.ts` — `normalizes uppercase ledger identifiers before reading and emitting documents` | PASS WITH RESTRICTION | Ledger hexadecimal identifiers are emitted in lowercase. |
| DID-005 | DID URL components remain distinct from a bare DID (§3, §6). | §2, §3.4.1, §3.6.1.1 | bare-DID schema; `packages/domain/src/did-url.ts` | `packages/domain/src/test/midnight-did-syntax.conformance.test.ts` — `rejects $label`; `packages/domain/src/test/did-url.test.ts` — `does not collapse distinct path references with the same fragment` | PASS WITH RESTRICTION | URL reference normalization is not dereferencing. |
| DOC-001 | A DID Document has a valid `id` (§7.1). | §3.2 | `packages/did/src/midnight-did-document.ts`; `packages/did/src/ledger-to-domain.ts` | `packages/did/src/test/midnight-did-document.test.ts` — `creates a valid Midnight DID Document with required contexts`; `packages/did/src/test/midnight-did-resolver.test.ts` — `resolves DID to document + metadata` | PASS | Resolver binds the ID to network and contract address. |
| DOC-002 | JSON-LD uses a valid DID representation context (§7.1, §7.2). | §3.1, §7.2.3 | `createMidnightDIDDocument`; `documentForRepresentation` | `packages/did/src/test/midnight-did-document.test.ts` — `creates a valid Midnight DID Document with required contexts`; `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `preserves resolver-stream JSON-LD semantics through expansion and compaction` | PASS WITH RESTRICTION | Midnight requires DID and JWK contexts. |
| DOC-003 | `controller` is a valid URI/DID (§7.1). | §3.2 | single-controller refinement in `packages/did/src/midnight-did-document.ts` | `packages/did/src/test/midnight-did-document.test.ts` — `sets controller equal to id (single-controller model)`; `rejects document where controller does not equal id` | PASS WITH RESTRICTION | Midnight requires controller equal to subject. |
| DOC-004 | `alsoKnownAs` values are URIs (§7.1). | §3.3 | `URIStringSchema`; ledger alias projection | `packages/did/src/test/midnight-did-document.test.ts` — `accepts non-DID URI values in alsoKnownAs`; `packages/did/src/test/midnight-did-resolver.test.ts` — `maps schema validation failures to invalidDid` | PASS | Invalid aliases fail projection. |
| DOC-005 | Optional properties are omitted or valid, never JSON `null` (§7.1). | §3.1, §3.5, §3.6 | DID document constructors and schemas | `packages/did/src/test/midnight-did-document.test.ts` — `omits absent optional members instead of emitting null`; `omits empty optional arrays`; `rejects null optional DID Document members`; `packages/domain/src/test/did-document.parse.test.ts` — `rejects DID Documents with null verification relationships` | PASS | Method constructors/parsers omit absent and empty optionals. |
| DOC-006 | Verification methods contain valid `id`, `type`, `controller`, and public material (§7.1). | §3.4 | verification-method schemas; `LedgerToDomain` | `packages/did/src/test/midnight-did-document.test.ts` — `rejects verification methods from another DID subject`; `rejects verification method with unsupported key type (RSA)`; `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `expands resolved DID Documents with every supported verification method profile` | PASS WITH RESTRICTION | Only local referenced `JsonWebKey` methods in supported profiles are accepted. |
| DOC-007 | Verification method IDs are unique and subject-bound (§7.1). | §3.4.1, §6.1 | `resolveDIDURLReference`; `validateDIDDocumentConsistency`; ledger mapper checks | `packages/did/src/test/midnight-did-document.test.ts` — `rejects duplicate verification method ids`; `rejects verification methods from another DID subject`; `packages/did/src/test/midnight-did-resolver.test.ts` — `returns deterministic malformed-ledger envelopes for $name`; `packages/did/src/test/ledger-to-domain.unit.test.ts` — `ledgerStateToDIDDocument classifies $name as $resolutionCode` | PASS WITH RESTRICTION | IDs are canonicalized before comparison. |
| DOC-008 | Public JWKs contain no private key material (§7.1). | §3.4.4, §5.3 | `packages/domain/src/did-document.ts` (`PublicKeyJwkSchema`) | `packages/domain/src/test/did-document.verification-method.test.ts` — `rejects public JWKs that include private key material` | PASS | The direct schema test supplies a JWK with `d` and asserts rejection. |
| VM-001 | Verification relationships reference valid, non-duplicate methods (§7.1). | §3.5 | `validateDIDDocumentConsistency`; ledger relation checks; API relation preflight | `packages/did/src/test/midnight-did-document.test.ts` — `rejects dangling verification relationships`; `packages/contract/src/test/did.test.ts` — `should fail when adding the same relation twice`; `packages/api/src/test/verification-method-relations.test.ts` — `guards duplicate and missing relation membership before submitting transactions` | PASS WITH RESTRICTION | Direct duplicate and missing-target evidence covers the local-method profile; external references are not claimed. |
| VM-002 | `authentication` is non-empty when present (§7.1). | §3.5.2 | relation schema and ledger mapping | `packages/domain/src/test/did-document.parse.test.ts` — `rejects DID Documents with empty verification relationship arrays`; `packages/contract/src/test/did.test.ts` — `should add Authentication relation` | PASS | Empty output is omitted. |
| VM-003 | `assertionMethod` is non-empty when present (§7.1). | §3.5.1 | relation schema and ledger mapping | `packages/contract/src/test/did.test.ts` — `should add AssertionMethod relation`; `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `expands resolved DID Documents with every supported verification method profile` | PASS | Empty output is omitted. |
| VM-004 | `keyAgreement` is non-empty and compatible when present (§7.1). | §3.5.5 | relation schema and contract curve guards | `packages/contract/src/test/did.test.ts` — `should add KeyAgreement relation`; `should reject signing keys in KeyAgreement relation`; `should reject X25519 keys in signing relations` | PASS WITH RESTRICTION | Current profile permits X25519 key agreement. |
| VM-005 | `capabilityInvocation` is non-empty when present (§7.1). | §3.5.3 | relation schema and ledger mapping | `packages/contract/src/test/did.test.ts` — `should add CapabilityInvocation relation` | PASS | Empty output is omitted. |
| VM-006 | `capabilityDelegation` is non-empty when present (§7.1). | §3.5.4 | relation schema and ledger mapping | `packages/contract/src/test/did.test.ts` — `should add CapabilityDelegation relation` | PASS | Empty output is omitted. |
| SRV-001 | Services contain valid `id`, `type`, and non-empty `serviceEndpoint` (§7.1). | §3.6 | service schemas; `LedgerToDomain.service`; consistency validation | `packages/did/src/test/midnight-did-document.test.ts` — `rejects empty service references`; `packages/did/src/test/midnight-did-resolver.test.ts` — `returns deterministic malformed-ledger envelopes for $name`; `maps an empty service endpoint set to invalidDid`; `maps duplicate service endpoints to invalidDid` | PASS | Endpoint sets are non-empty and unique after normalization. |
| SRV-002 | Service endpoints support string, object, and array forms (§7.1). | §3.6.1.3 | `ServiceEndpointSchema`; endpoint parser | `packages/domain/src/test/did-document.document-resolution.test.ts` — `accepts object service endpoints`; `creates services for DID Core endpoint variations`; `accepts multiple service endpoints` | PASS | This row is schema-level; lifecycle behavior is tested separately. |
| SRV-003 | Service IDs are unique and subject-bound (§7.1). | §3.6.1.1, §6.1 | DID URL reference resolution and service consistency checks | `packages/did/src/test/midnight-did-document.test.ts` — `rejects duplicate service ids`; `rejects relative service identifiers that normalize to duplicates`; `packages/did/src/test/midnight-did-resolver.test.ts` — `preserves distinct path services with the same fragment` | PASS WITH RESTRICTION | Historical foreign-DID services are read-only compatibility exceptions. |
| REP-001 | DID JSON is a valid equivalent representation (§7.2). | §3.1, §7.2.3 | `packages/did/src/midnight-did-resolver.ts` (`documentForRepresentation`) | `packages/did/src/test/midnight-did-resolver.test.ts` — `preserves the abstract DID data model through DID JSON serialization` | PASS | Parsed stream equals the abstract document without JSON-LD-only `@context`. |
| REP-002 | DID JSON-LD expands with valid terms (§7.2). | §3.1, §3.4.4, §7.2.3 | resolver representation stream; DID/JWK contexts | `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `expands resolved DID Documents with every supported verification method profile` | PASS WITH RESTRICTION | Deterministic local context documents avoid network-dependent evidence. |
| REP-003 | JSON-LD semantic round trip preserves the data model (§7.2). | §3.1, §7.2.3 | `resolveRepresentation`; deterministic JSON-LD loader in the cited test | `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `preserves resolver-stream JSON-LD semantics through expansion and compaction` | PASS WITH RESTRICTION | The test decodes the actual resolver stream, checks metadata/content type, expands, compacts, and re-expands. |
| RES-001 | The method defines create/read/update/deactivate (§7.1, §8). | §7 | Compact circuits, API operations, resolver | `packages/contract/src/test/did.test.ts` — `properly initializes ledger state and private state`; `should apply multiple operations sequentially`; `should deactivate the DID`; `packages/did/src/test/midnight-did-resolver.test.ts` — `resolves DID to document + metadata` | PASS WITH RESTRICTION | Recovery and rotation are method extensions; no generic batch workflow is claimed. |
| RES-002 | Create publishes initial DID state (§8). | §7.1 | contract constructor/deploy flow | `packages/contract/src/test/did.test.ts` — `properly initializes ledger state and private state` | PASS | Initial public and private state are asserted. |
| RES-003 | Read projects the DID state returned by the configured trusted reader into a DID Document (§8). | §7.2 | `MidnightDIDResolver`; `LedgerToDomain` | `packages/did/src/test/midnight-did-resolver.test.ts` — `resolves DID to document + metadata` | PASS WITH RESTRICTION | Bare-DID resolution only. The projection provides no freshness, finality, or independent-integrity guarantee beyond the configured reader's response. |
| RES-004 | Update is authorized and changes state (§8). | §7.3 | Compact mutations and API operations | `packages/contract/src/test/did.test.ts` — `authorizes controller updates with a wallet-local operation-bound signature`; `rejects replayed controller authorizations after the DID version changes`; `should apply multiple operations sequentially` | PASS WITH RESTRICTION | Each API operation is a separately finalized transaction. |
| RES-005 | Deactivation prevents later updates (§8). | §7.3.12, §4.3 | contract `deactivate`; metadata mapper | `packages/contract/src/test/did.test.ts` — `should deactivate the DID`; `rejects recovery after deactivation`; `rejects every mutating circuit after deactivation`; `packages/did/src/test/midnight-did-resolver.test.ts` — `returns deactivation metadata with the still-readable DID Document` | PASS | Public state remains readable; deactivation is irreversible. |
| SEC-001 | Security considerations cover authorization and key handling (§9). | §5, §8 | contract authorization/witnesses; API controller operations; method security text | `packages/contract/src/test/did.test.ts` — `rejects controller signatures reused with different operation arguments`; `rejects controller signatures for recovery operations`; `rejects recovery signatures for normal controller operations` | PASS WITH RESTRICTION | Custody, delegated proving, CMA, and provider trust remain operational responsibilities. |
| PRIV-001 | Privacy considerations identify ledger and metadata risks (§10). | §9 | public ledger architecture and method privacy text | No executable privacy test; direct evidence is `w3c-spec/midnight-method.md` §9. | DOCUMENTED RESTRICTION | This is a normative documentation obligation, not a unit-test claim. |
| COMP-001 | Method-specific compatibility behavior is explicit (§6, §8). | §6.1 | API canonical/legacy key selection; resolver historical projection | `packages/api/src/test/ledger-identifier-keys.test.ts` — `derives a legacy key only for an exact current-subject fragment identity`; `fails closed when canonical and legacy service keys coexist`; `packages/api/src/test/verification-method-operations.test.ts` — `updates an existing legacy fragment-keyed verification method`; `rejects ambiguous canonical and legacy method records before authorization` | PASS WITH RESTRICTION | No automatic historical-state migration is implied. |
| COMP-002 | Historical reads do not widen new-write policy (§6, §8). | §6.1 | domain parser, resolver projection, API ledger mappers | `packages/domain/src/test/did-document.parse.test.ts` — `parses fragment and historical path DID Key IDs`; `does not widen unrelated DID Key ID references`; `packages/did/src/test/midnight-did-resolver.test.ts` — `renders historical %s verification method ids without fragments`; `renders foreign-DID service ids already present in legacy ledger state`; `returns deterministic malformed-ledger envelopes for $name`; `maps network-path verification method ids to invalidDid` | PASS WITH RESTRICTION | Path-only method keys and foreign services are read-compatibility exceptions. |
| INT-001 | DID Core does not imply generic cryptographic interoperability (§7.1, §9). | §3.4.4 | explicit Jubjub/BLS/Multikey limitations | `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `expands resolved DID Documents with every supported verification method profile`; `packages/contract/src/test/did.test.ts` — `should add BLS12-381 OKP verification methods`; `should add, update, relate, and remove SchnorrJubjub verification methods` | KNOWN INTEROPERABILITY LIMITATION | Evidence proves Midnight profiles, not registry status or generic JOSE/Data Integrity support. |
| EXT-001 | DID URL resource/fragment dereferencing is characterized (§7.1; Resolution is separate). | §7.2 (bare resolution only) | No dereference API in `packages/did` or `packages/api` | No dereferencing test or operation exists. | NOT IMPLEMENTED | URL normalization and bare-DID resolution do not substitute for dereferencing; implementation is tracked by [#445](https://github.com/midnightntwrk/midnight-did/issues/445). |
