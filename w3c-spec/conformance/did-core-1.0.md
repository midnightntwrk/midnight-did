# DID Core 1.0 Conformance Matrix (0.6 Evidence)

**Baseline:** [W3C DID Core 1.0 Recommendation, 19 July 2022](https://www.w3.org/TR/2022/REC-did-core-20220719/). **0.6 starting commit:** `55dde88ce1e451587303b5f4fc388eef4bdb32f5`.

This matrix groups related requirements into executable evidence rows. `PASS` means only that the exact cited tests prove the stated behavior within their scope; it is not W3C certification or a claim of complete DID Core compatibility.

| ID       | Requirement / method boundary                                                                                                          | Exact executable evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status                            | Notes                                                                                                                   |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| DID-001  | Ledger and offchain method-specific DID syntax is validated and ledger hex is canonicalized to lowercase.                              | `packages/domain/src/test/midnight-did-syntax.conformance.test.ts` — `accepts ledger DID %s`, `accepts both offchain short and syntactically valid long forms`, `canonicalizes ledger hexadecimal case to lowercase`, `rejects $label`                                                                                                                                                                                                                                                                                                          | PASS WITH RESTRICTION             | The parser is intentionally `did:midnight`-specific.                                                                    |
| DID-002  | Bare DID parsing is distinct from DID URL components.                                                                                  | `packages/domain/src/test/midnight-did-syntax.conformance.test.ts` — `rejects $label`; `packages/domain/src/test/did-url.test.ts` — `does not collapse distinct path references with the same fragment`                                                                                                                                                                                                                                                                                                                                         | PASS WITH RESTRICTION             | Bare DID parsing rejects path, query, and fragment inputs; reference resolution is a separate domain helper.            |
| DID-003  | DID URL references preserve path/query/fragment identity and remain subject-bound by default.                                          | `packages/domain/src/test/did-url.test.ts` — `normalizes dot segments in absolute DID URLs`, `rejects foreign DID URLs by default`, `rejects network-path references`; `packages/did/src/test/midnight-did-document.test.ts` — `resolves path and query DID URL references losslessly`                                                                                                                                                                                                                                                          | PASS WITH RESTRICTION             | External and foreign-DID profiles are accepted only where explicitly allowed. This is normalization, not dereferencing. |
| DOC-001  | Resolved Midnight DID Documents have the required `id`, contexts, and single-controller profile.                                       | `packages/did/src/test/midnight-did-document.test.ts` — `creates a valid Midnight DID Document with required contexts`, `sets controller equal to id (single-controller model)`; `packages/did/src/test/midnight-did-resolver.test.ts` — `resolves DID to document + metadata`                                                                                                                                                                                                                                                                  | PASS WITH RESTRICTION             | Controller equality is a Midnight method rule.                                                                          |
| DOC-002  | Aliases and services are represented as valid data-model values.                                                                       | `packages/did/src/test/midnight-did-document.test.ts` — `accepts non-DID URI values in alsoKnownAs`, `includes service endpoints when provided`                                                                                                                                                                                                                                                                                                                                                                                                 | PASS                              | Ledger lifecycle behavior is covered by the service and alias contract tests cited below.                               |
| DOC-003  | Absent/empty optional members are omitted and explicit JSON `null` is rejected.                                                        | `packages/did/src/test/midnight-did-document.test.ts` — `omits absent optional members instead of emitting null`, `omits empty optional arrays`, `rejects null optional DID Document members`; `packages/domain/src/test/did-document.parse.test.ts` — `rejects DID Documents with null verification relationships`                                                                                                                                                                                                                             | PASS                              | This is the 0.6 parsing/production policy; persisted 0.5 documents with null optionals require cleanup before parsing.  |
| VM-001   | Verification methods require supported public JWK material, contain no private material, and are unique/subject-bound.                 | `packages/did/src/test/midnight-did-document.test.ts` — `rejects verification methods from another DID subject`, `rejects duplicate verification method ids`, `rejects verification method with unsupported key type (RSA)`; `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `expands resolved DID Documents with every supported verification method profile`                                                                                                                                                                | PASS WITH RESTRICTION             | Current output uses `JsonWebKey`; Multikey is not claimed.                                                              |
| VM-002   | Verification relationships resolve to local methods, reject dangling/duplicate targets, and enforce key-agreement curve compatibility. | `packages/did/src/test/midnight-did-document.test.ts` — `rejects dangling verification relationships`; `packages/domain/src/test/did-document.parse.test.ts` — `rejects DID Documents with empty verification relationship arrays`; `packages/contract/src/test/did.test.ts` — `should reject signing keys in KeyAgreement relation`, `should reject X25519 keys in signing relations`                                                                                                                                                          | PASS WITH RESTRICTION             | External verification-method references are outside the current method profile.                                         |
| SRV-001  | Services have valid IDs, types, and non-empty endpoint values; IDs are unique after normalization.                                     | `packages/did/src/test/midnight-did-document.test.ts` — `rejects duplicate service ids`, `rejects relative service identifiers that normalize to duplicates`, `rejects empty service references`; `packages/did/src/test/midnight-did-resolver.test.ts` — `maps an empty service endpoint set to invalidDid`, `maps duplicate service endpoints to invalidDid`                                                                                                                                                                                  | PASS                              | Endpoint object/array/string forms remain schema-governed.                                                              |
| COMP-001 | New IDs use complete canonical identity; exact legacy current-subject fragment keys remain operable and ambiguity fails closed.        | `packages/api/src/test/ledger-identifier-keys.test.ts` — `derives a legacy key only for an exact current-subject fragment identity`, `fails closed when canonical and legacy service keys coexist`; `packages/api/src/test/verification-method-operations.test.ts` — `updates an existing legacy fragment-keyed verification method`, `rejects ambiguous canonical and legacy method records before authorization`                                                                                                                              | PASS WITH RESTRICTION             | This preserves #434 without automatic state migration.                                                                  |
| COMP-002 | Historical path/dot-relative method IDs and foreign-DID service IDs are read-compatible without widening new writes.                   | `packages/domain/src/test/did-document.parse.test.ts` — `parses fragment and historical path DID Key IDs`, `does not widen unrelated DID Key ID references`; `packages/did/src/test/midnight-did-resolver.test.ts` — `renders historical %s verification method ids without fragments`, `renders foreign-DID service ids already present in legacy ledger state`, `maps network-path verification method ids to invalidDid`; `packages/api/src/test/ledger-mappers.test.ts` — `preserves bound service URLs and rejects new foreign-DID writes` | PASS WITH RESTRICTION             | Historical path-only physical method keys are read-only through current mutation helpers.                               |
| REP-001  | DID JSON serialization preserves the abstract DID data model while omitting representation-specific `@context`.                        | `packages/did/src/test/midnight-did-resolver.test.ts` — `preserves the abstract DID data model through DID JSON serialization`                                                                                                                                                                                                                                                                                                                                                                                                                  | PASS                              | The test compares the parsed stream, metadata, and abstract resolution document.                                        |
| REP-002  | DID JSON-LD expands under deterministic DID/JWK contexts and preserves expanded semantics through compaction.                          | `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `expands resolved DID Documents with every supported verification method profile`, `preserves JSON-LD semantics through expansion and compaction`, `documents that JsonWebKey2020 is not the Midnight JSON-LD verification method type`                                                                                                                                                                                                                                       | PASS WITH RESTRICTION             | Deterministic local context documents avoid network-dependent tests.                                                    |
| RES-001  | Create/read/update operations are authorized and mutate versioned ledger state.                                                        | `packages/contract/src/test/did.test.ts` — `properly initializes ledger state and private state`, `authorizes controller updates with a wallet-local operation-bound signature`, `rejects replayed controller authorizations after the DID version changes`, `should apply multiple operations sequentially`                                                                                                                                                                                                                                    | PASS WITH RESTRICTION             | One API operation maps to one independently finalized transaction; no batch/atomic workflow is claimed.                 |
| RES-002  | Deactivation is irreversible, leaves public state readable, and blocks later mutation/recovery.                                        | `packages/contract/src/test/did.test.ts` — `should deactivate the DID`, `rejects recovery after deactivation`, `rejects every mutating circuit after deactivation`; `packages/did/src/test/midnight-did-resolver.test.ts` — `returns deactivation metadata with the still-readable DID Document`                                                                                                                                                                                                                                                | PASS                              | Deactivation is not erasure or a substitute for key recovery.                                                           |
| SEC-001  | Controller and recovery authorization are operation-bound and wallet-local.                                                            | `packages/contract/src/test/did.test.ts` — `rejects controller signatures reused with different operation arguments`, `rejects controller signatures for recovery operations`, `rejects recovery signatures for normal controller operations`; `packages/api/src/test/controller-operations.test.ts` — `rotates to a locally derived controller public key and stores the new secret`, `recovers to a locally derived controller public key with recovery private state`                                                                        | PASS WITH RESTRICTION             | Custody, delegated proving, CMA, and indexer trust boundaries remain documented operational responsibilities.           |
| INT-001  | Jubjub, BLS JWK names, and absent Multikey do not imply generic ecosystem interoperability.                                            | `packages/did/src/test/midnight-did-jsonld-conformance.test.ts` — `expands resolved DID Documents with every supported verification method profile`; `packages/contract/src/test/did.test.ts` — `should add BLS12-381 OKP verification methods`, `should add, update, relate, and remove SchnorrJubjub verification methods`                                                                                                                                                                                                                    | KNOWN INTEROPERABILITY LIMITATION | These tests prove the Midnight profiles, not registration or generic JOSE/Data Integrity support.                       |
| EXT-001  | DID URL resource/fragment dereferencing.                                                                                               | No dereference operation exists in `packages/did` or `packages/api`; bare-DID syntax tests reject URL components.                                                                                                                                                                                                                                                                                                                                                                                                                               | NOT IMPLEMENTED                   | This remains an explicit limitation, not `PASS` and not an unresolved evidence row.                                     |

Privacy and operational trust considerations are specified in `w3c-spec/midnight-method.md`. They are documentation obligations rather than claims that can be proven by a unit test.
